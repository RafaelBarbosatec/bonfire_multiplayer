// ignore_for_file: constant_identifier_names

enum EventType {
  LOGIN,
  LOGIN_RESPONSE,
  REGISTER,
  REGISTER_RESPONSE,
  JOIN,
  JOIN_ACK,
  UPDATE_STATE,
  PLAYER_JOIN,
  PLAYER_LEAVE,
  PLAYER_MOVE,
  MOVE_RESPONSE,
}
